class EventTeamStatus:
    """Class representing a team's status during an event."""

    def __init__(self, event_key: str, team_status_info: dict):
        self.event_key = event_key
        print(team_status_info)
